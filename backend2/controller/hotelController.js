// const Hotels = require("../models/Hotels");

// export const createHotels = async (req, res) => {
//   const { img, title, description } = req.body;
//   const hotel = await Hotels.create({ img, title, description });
//   res.status(201).json({ hotel });
// };
